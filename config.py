# File containing config

api_port = 1234
database_connection_uri = "CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY AUTOINCREMENT, username text, password text)"
secret_key = "CREATE TABLE IF NOT EXISTS transaction_model (name text, description text, traduction text)"

